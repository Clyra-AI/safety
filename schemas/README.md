# Schemas

Versioned schema contracts for datasets and claim ledgers.

Current:

- `claims.schema.json`: headline claim mapping contract

Add new schemas using semantic versioned filenames (for example `inventory-rows.v1.schema.json`).
