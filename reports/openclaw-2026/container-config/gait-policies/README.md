# Gait Policies

Store exact policy files used for the governed OpenClaw run.

Keep policy filenames immutable once referenced in a published report.

Default starter policy file:

- `openclaw-research-v1.yaml` (must be validated against active Gait policy schema before execution)
